# 下棋 AI 与博弈搜索功能规划

## 1. 目标

在现有 Mini ReAct Agent 中加入一个可交互的棋类 AI，使用户说“想下棋”时，智能体能够调用棋局工具创建棋盘，并在 Web 页面中展示可点击棋盘。用户落子后点击“AI 下棋”，系统调用下棋工具完成决策；棋局结束后展示胜负结果，并调用分析工具给出复盘说明。

本功能对应选题三“下棋 AI 与博弈搜索”，重点展示经典 AI 中的 Minimax、Alpha-Beta 剪枝、启发式评估和迭代加深搜索。

## 2. 棋种选择

首版选择 9x9 小棋盘五子棋。

选择理由：

- 规则直观，用户能快速理解并开始对弈。
- 状态表示、合法走法、胜负判断都适合用二维数组实现。
- 搜索空间比井字棋更有挑战，也比标准 15x15 五子棋更容易在 5 秒内完成高质量决策。
- 评估函数可以体现棋型判断能力，例如连二、活三、冲四、活四、成五。

默认规则：

- 棋盘大小：9x9。
- 玩家执黑先手，AI 执白后手。
- 任意方向横、竖、左斜、右斜连成 5 子即胜。
- 不实现禁手规则，降低首版复杂度。
- 棋盘填满且无人五连则判平局。

## 3. 用户流程

1. 用户在聊天框输入“我想下棋”“来一盘五子棋”等自然语言。
2. 智能体识别意图，调用 `game_create` 工具创建棋局。
3. 前端收到棋局状态后显示棋盘、当前回合、操作按钮。
4. 用户点击棋盘格落子。
5. 用户点击“AI 下棋”按钮。
6. 前端把最新棋局状态发给后端，后端调用 `game_ai_move` 工具。
7. AI 使用迭代加深 Minimax + Alpha-Beta 剪枝在时限内选出落子。
8. 前端更新棋盘，继续人机轮流对弈。
9. 棋局结束时，显示胜、负、平结果，并自动调用 `game_analyze` 工具输出棋局复盘。
10. 用户可随时点击“提前退出”结束当前棋局，系统保留已有棋谱并给出简短总结。

## 4. 新增工具设计

工具统一放入 `tools.py` 注册表，但核心逻辑建议拆到独立模块，避免 `tools.py` 继续膨胀。

建议新增文件：

- `game.py`：棋局状态、规则、胜负判断、棋谱记录。
- `game_ai.py`：Minimax、Alpha-Beta、迭代加深、评估函数。
- `game_tools.py`：面向 Agent 的工具包装函数。

### 4.1 `game_create`

用途：创建一局新的五子棋。

输入：

```json
{
  "game": "gomoku",
  "size": 9,
  "human": "B",
  "ai": "W"
}
```

输出：

```json
{
  "game_id": "uuid",
  "game": "gomoku",
  "size": 9,
  "board": [[0, 0, 0]],
  "human": 1,
  "ai": 2,
  "turn": 1,
  "status": "playing",
  "winner": 0,
  "moves": [],
  "message": "五子棋已开始，玩家执黑先手。"
}
```

棋子编码：

- `0`：空位
- `1`：玩家黑棋
- `2`：AI 白棋

### 4.2 `game_player_move`

用途：记录玩家落子，并返回更新后的棋局。

输入：

```json
{
  "game_id": "uuid",
  "row": 4,
  "col": 4
}
```

校验：

- 棋局存在。
- 棋局仍在进行。
- 当前轮到玩家。
- 坐标在棋盘内。
- 目标格为空。

输出：更新后的棋局状态。若玩家胜利或棋盘已满，直接返回终局状态。

### 4.3 `game_ai_move`

用途：让 AI 决策并落子。

输入：

```json
{
  "game_id": "uuid",
  "time_limit_ms": 5000,
  "max_depth": 5
}
```

输出：

```json
{
  "game_id": "uuid",
  "move": {"row": 4, "col": 5},
  "depth_reached": 4,
  "nodes": 13240,
  "score": 860,
  "elapsed_ms": 3180,
  "board": [[0, 0, 0]],
  "status": "playing",
  "winner": 0,
  "message": "AI 落子在 E5。"
}
```

### 4.4 `game_analyze`

用途：棋局结束或提前退出时分析棋局。

输入：

```json
{
  "game_id": "uuid"
}
```

输出：

```json
{
  "result": "AI 胜",
  "summary": "AI 在中盘通过冲四迫使玩家防守，并在右上方向形成连续威胁。",
  "key_moves": [
    {"move_no": 8, "comment": "AI 建立了第一条活三。"},
    {"move_no": 13, "comment": "玩家没有及时封堵冲四。"}
  ],
  "metrics": {
    "total_moves": 18,
    "max_depth": 4,
    "average_ai_elapsed_ms": 2100
  }
}
```

### 4.5 `game_resign`

用途：玩家提前退出当前棋局。

输入：

```json
{
  "game_id": "uuid"
}
```

输出：将棋局状态设为 `resigned`，再触发或提示调用 `game_analyze`。

## 5. 棋局状态表示

核心数据结构使用 Python dataclass。

```python
@dataclass
class GameState:
    game_id: str
    size: int
    board: list[list[int]]
    human: int
    ai: int
    turn: int
    status: str
    winner: int
    moves: list[Move]
    created_at: float
    updated_at: float
```

建议暂存在内存字典：

```python
GAMES: dict[str, GameState] = {}
```

首版不强制持久化。后续若需要恢复历史棋局，可写入 `data/games.json`。

## 6. 规则模块

`game.py` 需要提供以下能力：

- `new_game(size=9)`：创建空棋盘。
- `legal_moves(state)`：生成合法空位。
- `apply_move(state, row, col, player)`：返回或原地更新棋局。
- `check_winner(board, row, col)`：从最后一步出发检查四个方向是否五连。
- `is_full(board)`：判断是否平局。
- `serialize_state(state)`：转换为 JSON 友好的字典。

胜负判断方向：

- `(0, 1)`：横向
- `(1, 0)`：纵向
- `(1, 1)`：左上到右下
- `(1, -1)`：右上到左下

## 7. 搜索算法

### 7.1 Minimax

AI 视角：

- AI 节点最大化分数。
- 玩家节点最小化分数。
- 如果搜索到终局，直接返回极大正分、极大负分或 0。
- 如果达到深度限制，用评估函数估计局面。

### 7.2 Alpha-Beta 剪枝

在 Minimax 参数中加入 `alpha` 和 `beta`：

- 最大化节点更新 `alpha`。
- 最小化节点更新 `beta`。
- 当 `alpha >= beta` 时停止展开该分支。

### 7.3 迭代加深

`game_ai_move` 在时间限制内从深度 1 开始逐步加深：

1. 深度 1 搜索，得到一个可用走法。
2. 若未超时，继续深度 2。
3. 每完成一层，保存当前最佳走法。
4. 时间接近上限时返回最后一次完整深度的最佳走法。

时间控制：

- 默认 `time_limit_ms=5000`。
- 搜索内部每隔一定节点数检查时间。
- 如果超时，抛出内部 `SearchTimeout`，外层返回已保存的最佳结果。

## 8. 走法生成优化

五子棋不应展开所有空位。首版采用“邻域候选点”：

- 如果棋盘为空，候选点为中心点。
- 否则只生成已有棋子周围 2 格范围内的空位。
- 候选点按启发式排序，优先考虑：
  - 能直接成五的位置。
  - 能阻止对方成五的位置。
  - 形成活四、冲四、活三的位置。
  - 靠近棋盘中心的位置。

这样能显著提高 Alpha-Beta 剪枝效率。

## 9. 评估函数

评估函数返回“对 AI 有利程度”。

基础思路：

```text
score = AI 棋型得分 - 玩家棋型得分
```

建议棋型分值：

- 五连：`1_000_000`
- 活四：`100_000`
- 冲四：`10_000`
- 活三：`5_000`
- 眠三：`800`
- 活二：`300`
- 眠二：`80`
- 单子与中心控制：`1~20`

棋型统计方式：

- 扫描所有横、竖、两条斜线。
- 把线上的棋子转换为字符串，例如 AI 为 `A`，玩家为 `H`，空位为 `_`。
- 匹配常见模式，例如 `_AAAA_`、`HAAAA_`、`_AAA__`。
- 同一威胁可能重复计分，首版接受轻微重复，后续再做去重优化。

紧急规则：

- 如果 AI 当前有一步成五，直接落该点。
- 如果玩家下一步能成五，优先堵住。
- 如果 AI 可形成活四，优先级高于普通深搜评估。

## 10. 前端交互设计

`web_app.py` 当前是内嵌 HTML/JS。首版可继续在同一页面中增加棋局面板。

界面元素：

- 9x9 棋盘网格。
- 黑白棋子显示。
- 当前回合提示。
- “AI 下棋”按钮。
- “提前退出”按钮。
- 终局结果区域。
- 分析结果区域。

交互约定：

- 用户点击空格后，前端调用 `/api/game/player_move`。
- 玩家落子成功后，“AI 下棋”按钮变为可用。
- 点击“AI 下棋”后调用 `/api/game/ai_move`，期间禁用棋盘和按钮。
- 如果工具返回终局状态，显示结果并自动调用 `/api/game/analyze`。
- 提前退出调用 `/api/game/resign`，随后调用分析。

前端 API 建议：

- `POST /api/game/create`
- `POST /api/game/player_move`
- `POST /api/game/ai_move`
- `POST /api/game/analyze`
- `POST /api/game/resign`

这些 API 可以直接调用底层游戏函数，不必强制绕一遍 LLM。聊天中的 Agent 工具调用负责“自然语言触发创建棋局”，按钮交互负责稳定、低延迟的游戏操作。

## 11. Agent 提示词调整

`prompts.py` 需要加入新工具说明：

- 当用户表达“想下棋 / 来一盘 / 五子棋 / 和 AI 对战”时，优先调用 `game_create`。
- 不要自己编造棋盘状态，必须使用工具返回的状态。
- 当用户询问当前棋局、要求 AI 走一步、要求复盘时，分别调用对应工具。
- 工具结果中若包含 `status=finished`、`won`、`draw`、`resigned`，需要说明结果并建议查看分析。

## 12. 实现顺序

1. 新建 `game.py`，完成棋盘、合法走法、落子、胜负判断。
2. 新建 `game_ai.py`，实现候选走法、评估函数、Minimax、Alpha-Beta、迭代加深。
3. 新建 `game_tools.py`，包装 `game_create`、`game_player_move`、`game_ai_move`、`game_analyze`、`game_resign`。
4. 修改 `tools.py`，把新工具加入 `TOOLS`。
5. 修改 `prompts.py`，让 Agent 会在自然语言场景调用棋局工具。
6. 修改 `web_app.py`，增加棋盘 UI 和 `/api/game/*` 接口。
7. 增加测试或脚本验证：
   - 玩家非法落子会被拒绝。
   - 五连胜负判断正确。
   - AI 能在空棋盘优先下中心。
   - AI 能立即成五。
   - AI 能阻挡玩家立即成五。
   - 每步搜索时间不超过 5 秒。

## 13. 验收标准

- 用户说“我想下五子棋”后，聊天中出现棋局创建结果，页面显示可交互棋盘。
- 玩家能点击棋盘落子，非法落子会提示错误。
- 点击“AI 下棋”后，AI 在 5 秒内返回合法落子。
- AI 至少具备明显防守能力：能堵住玩家下一手五连。
- AI 至少具备明显进攻能力：有一步成五时会立即获胜。
- 棋局结束时能明确显示“玩家胜 / AI 胜 / 平局 / 玩家退出”。
- 终局或退出后能调用分析工具，给出简短复盘和关键手说明。

## 14. 后续可扩展方向

- 支持人类选择先手或后手。
- 支持 8x8、9x9、11x11 多种棋盘大小。
- 保存棋谱到 `data/`，支持历史棋局回放。
- 加入难度档位：简单、中等、困难，对应不同搜索深度和时间限制。
- 前端显示 AI 搜索信息，例如搜索深度、节点数、耗时、评分。
- 增加黑白棋或井字棋作为可选棋种，复用同一套 `GameState` 和工具协议。
