# Spec: 五子棋棋盘扩展与 AI 适配升级规划

## 1. 文档目标

本文档用于规划当前仅面向 `9x9` 小棋盘体验优化的五子棋功能，扩展为支持多档棋盘规格的人机对弈系统，并同步制定 AI 算法升级方案。规划重点覆盖：

- 棋盘规格扩展范围与推荐落地顺序
- 前后端核心模块的改造点、优先级与里程碑
- AI 在大棋盘场景下的算法适配、性能目标与优化路线
- 分阶段实施计划与测试验证方案

本文档定位为专项规划文档，不直接修改现有业务逻辑。

---

## 2. 现状审查

### 2.1 当前功能边界

当前项目对外呈现的是一套 `9x9` 五子棋体验，核心事实如下：

- 前端创建棋局时写死 `size: 9`
- 前端空棋盘默认渲染 `9x9`
- 前端工具说明文案写死为“创建 9x9 五子棋”
- 五子棋 AI 的搜索参数与启发式权重是围绕 `9x9` 小棋盘调优的
- 自动落子时默认参数为 `time_limit_ms=1200`、`max_depth=4`
- 现有测试数据与中心开局断言均以 `9x9` 棋盘为基准

### 2.2 当前代码已具备的可扩展基础

虽然产品层面当前只支持 `9x9`，但后端已有部分扩展基础：

- `game.py` 的 `GameState` 已使用 `size` 字段描述棋盘规模
- `new_game(size=9)` 已支持传入动态尺寸
- 棋盘数据结构使用 `list[list[int]]`，天然支持 N x N 扩展
- 边界判断 `in_bounds()`、合法落子生成、胜负判定并未写死 `9`
- `serialize_state()` 会把 `size` 连同 `board` 一并输出

### 2.3 当前真正限制扩展的瓶颈

当前限制多规格落地的核心问题不在棋盘数据结构本身，而在以下三类耦合：

1. 产品与前端层耦合
   - 创建棋局接口调用写死 `size: 9`
   - 棋盘默认渲染尺寸写死 `9`
   - 文案、流程与测试都默认“小棋盘快速对弈”

2. AI 参数与搜索策略耦合
   - `MAX_CANDIDATES=20`、固定邻域候选、固定深度与时限仅适合 `9x9`
   - 当前 `minimax + alpha-beta + 启发式评估` 在 `13x13` 及以上会面临分支爆炸

3. 质量基线缺失
   - 没有针对 `13x13 / 15x15 / 19x19` 的回归测试
   - 没有性能基准和棋力基准
   - 没有按棋盘规格划分 AI 参数配置

---

## 3. 目标与非目标

### 3.1 目标

- 将五子棋从单一 `9x9` 规格扩展为多规格棋盘系统
- 支持主流棋盘规格的产品化规划：`13x13`、`15x15`、`19x19`
- 保持统一规则语义：连五判胜、满盘平局、不引入禁手
- 在 `13x13`、`15x15` 上实现稳定可用的人机对弈体验
- 在扩展过程中保证 AI 单步响应时长满足用户体验目标：
  - 默认目标：`<= 3 秒`
  - 峰值容忍：`<= 5 秒`
- AI 棋力不因棋盘扩大出现“明显降级”为随机落子或纯邻近落子

### 3.2 非目标

- 首阶段不引入职业规则中的黑方禁手、长连禁手、三三禁手、四四禁手
- 首阶段不做联机对战或房间制多人对弈
- 首阶段不做持久化棋谱数据库重构
- 首阶段不要求 `19x19` 达到与 `15x15` 相同的实时对弈稳定性

---

## 4. 可支撑棋盘尺寸范围评估

### 4.1 调研结论

从五子棋常见实践和本项目当前架构约束综合判断，可分为三个层次：

- **当前架构的立即可扩展范围**：`9x9 ~ 15x15`
- **当前同步交互模型下的推荐产品范围**：`9x9 / 13x13 / 15x15`
- **需要 AI 引擎升级后再开放的实验范围**：`19x19`

### 4.2 最大可支撑尺寸判断

在不引入独立搜索服务、GPU 推理或大规模并行搜索的前提下：

- **工程上可维护的稳定上限**：`15x15`
- **可实验接入但不建议首发默认开放的上限**：`19x19`

原因如下：

- 当前前后端架构对棋盘尺寸本身没有硬性限制
- 但 AI 搜索复杂度会随着棋盘扩大显著上升
- `19x19` 在维持 `<= 3 秒` 响应目标时，需要更强的候选筛选、搜索裁剪和算法混合策略
- 若继续沿用当前 9x9 参数，仅放大棋盘尺寸，会导致：
  - 搜索深度明显下降
  - 棋力劣化
  - 中后盘响应时间不稳定

### 4.3 可选规格方案对比

| 规格 | 场景定位 | 实现成本 | AI 性能压力 | 用户体验 | 结论 |
| --- | --- | --- | --- | --- | --- |
| `9x9` | 教学、演示、快速体验 | 低 | 低 | 对局快，反馈快 | 保留为快速模式 |
| `13x13` | 轻量专业模式、兼顾棋感与速度 | 中 | 中 | 比 9x9 更接近标准体验，仍可控 | 第一阶段优先扩展 |
| `15x15` | 主流通用五子棋规格 | 中高 | 高 | 专业性更强，是推荐正式规格 | 第二阶段主目标 |
| `19x19` | 兼容围棋棋盘场景、研究型模式 | 高 | 很高 | 局面更大，但对现有 AI 压力明显 | 第三阶段实验性支持 |

### 4.4 规格方案建议

推荐采用“三档位分层开放”策略：

1. **快速模式**：`9x9`
   - 面向首次体验用户
   - AI 响应最快
   - 适合课堂演示和功能验证

2. **标准模式**：`13x13`
   - 作为第一阶段新增规格
   - 是算法升级后最稳妥的扩展目标
   - 可兼顾棋力、时延与前端可视化

3. **专业模式**：`15x15`
   - 作为第二阶段正式规格
   - 更接近通用五子棋体验
   - 需要更强的 AI 候选裁剪与参数管理

4. **实验模式**：`19x19`
   - 第三阶段按实验功能引入
   - 默认关闭或标记“高耗时”
   - 需要混合算法或异步搜索策略

---

## 5. 前后端改造范围梳理

## 5.1 前端改造模块

### A. 棋盘创建入口

现状：

- 创建棋局接口请求固定为 `{game: "gomoku", size: 9}`

改造内容：

- 增加棋盘规格选择控件
- 在创建请求中透传用户选择的 `size`
- 维护当前会话的棋盘规格状态

优先级：P0

### B. 棋盘渲染逻辑

现状：

- 默认空棋盘写死为 `9x9`
- CSS 最大宽度固定，小格子数量增大后可视性下降

改造内容：

- 将默认棋盘占位改为基于 `size` 动态生成
- 根据 `size` 自适应：
  - 棋盘容器宽度
  - 单格最小尺寸
  - 棋子半径
  - 间距与边框
- 对 `15x15`、`19x19` 增加响应式缩放方案

优先级：P0

### C. 坐标系统与展示文案

现状：

- 当前坐标仍以“几行几列”为主
- 文案中写死“创建 9x9 五子棋”

改造内容：

- 文案从写死 `9x9` 改为动态规格
- 棋盘展示支持 `A1` 风格或“行列双制”
- 复盘文本、落子提示、工具步骤说明统一使用动态规格

优先级：P1

### D. 落子边界与交互事件绑定

现状：

- 点击事件绑定本身已按二维循环生成，可自然扩展

改造内容：

- 校验大棋盘下格子点击热区是否过小
- 对 `19x19` 增加触屏友好性方案
- 在大棋盘下优化禁用态、等待态和 AI 搜索中的交互提示

优先级：P1

### E. 前端性能与可用性优化

改造内容：

- 避免每步全量重建大量 DOM 带来的卡顿
- 对 `15x15`、`19x19` 优先考虑局部更新或轻量重绘
- 增加 AI 思考状态、规格标签、性能提示

优先级：P2

## 5.2 后端改造模块

### A. 棋盘数据结构重构

现状：

- `GameState.size` 和 `board` 已可支持动态 N x N

改造内容：

- 将“逻辑支持”升级为“产品级支持”
- 为不同规格建立统一配置对象，例如：
  - 默认时间限制
  - 默认最大深度
  - 默认候选数上限

优先级：P0

### B. 胜负判定与边界逻辑

现状：

- `check_winner()`、`in_bounds()` 已可适配 N x N

改造内容：

- 增加多规格回归测试，验证在 `13x13 / 15x15 / 19x19` 下无回归
- 检查坐标标签 `coord_label()` 在更大棋盘下的命名策略
- 为未来超过 26 列的扩展预留双字母列名策略

优先级：P0

### C. 存储与状态管理

现状：

- 棋局状态保存在内存字典 `GAMES`

改造内容：

- 保持内存态不变，但把 `size` 视为一等公民字段贯穿所有 API
- 分离 AI 指标与棋谱数据的输出结构
- 为未来持久化棋谱预留规格字段

优先级：P1

### D. 工具层与 API 适配

改造内容：

- `game_create` 允许外部传入规格选项
- `game_ai_move` 根据当前规格加载对应 AI 配置
- `game_analyze` 输出中补充棋盘规格、策略档位和性能指标

优先级：P0

---

## 6. 改造优先级与里程碑

### 6.1 优先级定义

- P0：不做就无法支持多规格
- P1：影响体验一致性与产品完成度
- P2：提升性能、可维护性或高级体验

### 6.2 模块优先级汇总

| 模块 | 内容 | 优先级 |
| --- | --- | --- |
| 前端创建入口 | 动态选择规格并透传 `size` | P0 |
| 前端棋盘渲染 | 动态格数、自适应尺寸 | P0 |
| 后端规格配置 | 不同规格对应不同 AI 默认参数 | P0 |
| 工具层 | `game_create / game_ai_move` 动态规格适配 | P0 |
| 胜负判定回归 | 多规格规则校验 | P0 |
| 文案与坐标系统 | 动态规格文案、坐标统一 | P1 |
| 存储与棋谱结构 | 规格字段贯穿输出和分析 | P1 |
| 前端交互细节 | 大棋盘点击热区、状态提示 | P1 |
| DOM 局部更新 | 大棋盘前端性能优化 | P2 |
| 19x19 实验模式 | 高规格实验能力 | P2 |

### 6.3 里程碑节点

#### 里程碑 M1：多规格基础能力打通

- 前端支持选择 `9x9 / 13x13 / 15x15`
- 后端支持多规格创建、渲染、落子、胜负判断
- AI 参数可按规格切换
- `13x13` 达到可玩状态

#### 里程碑 M2：标准规格稳定化

- `15x15` 达到主推可玩状态
- AI 响应 `<= 3 秒` 达到 90% 分位目标
- 增加分析与性能面板

#### 里程碑 M3：实验规格与高级优化

- `19x19` 实验支持
- MCTS 或混合策略试点
- 进一步优化搜索缓存、并行与异步体验

---

## 7. AI 升级专项规划

## 7.1 当前 AI 算法结构

当前 AI 路线是：

- 必胜点与堵截点优先
- 候选点筛选
- 迭代加深
- Minimax
- Alpha-Beta 剪枝
- 启发式局面评估

这条路线在 `9x9` 小棋盘上是合理的，但向大棋盘扩展时会出现明显瓶颈。

## 7.2 现有算法瓶颈分析

### A. 分支因子快速增大

虽然当前只在已有棋子附近生成候选点，但棋盘扩大后：

- 有效候选点数量会持续增加
- 中盘以后 `MAX_CANDIDATES=20` 可能不够，也可能选得不准
- 若简单增大候选数，搜索树会迅速膨胀

### B. 深度与时延难同时保证

在 `9x9` 上可接受的深度，在 `15x15` 或 `19x19` 上会明显缩水：

- 深度不够会导致棋力下降
- 深度提高又会导致超时

### C. 评估函数面向小棋盘调优

当前启发式分值对：

- 活四
- 冲四
- 活三
- 眠三
- 活二

等模式进行加权，但在大棋盘中需要更重视：

- 中央控制
- 多战线威胁构建
- 远程连通潜力
- 开局布局质量

### D. 当前无缓存与转置表

当前搜索过程中没有引入：

- Zobrist Hash
- Transposition Table
- 历史启发排序
- Killer Move

因此会重复评估大量相似局面。

## 7.3 AI 算法选型评估

### 路线 A：增强版 Minimax + Alpha-Beta

升级方向：

- 按规格动态调整候选点数量与搜索深度
- 增加 move ordering
- 加入迭代加深主变排序
- 引入 transposition table
- 引入 killer move / history heuristic
- 优化评估函数

优点：

- 与当前代码延续性最好
- 改造成本相对可控
- 对 `13x13 / 15x15` 最现实

缺点：

- 对 `19x19` 仍有上限

结论：

- 作为第一、第二阶段主路线，优先级最高

### 路线 B：MCTS（蒙特卡洛树搜索）

升级方向：

- 以 UCT 为核心
- 结合快速 rollout 或启发式 rollout
- 可按时间预算稳定输出结果

优点：

- 更适合超大搜索空间
- 时间预算友好，天然适合“给我 3 秒最优解”

缺点：

- 纯 MCTS 在无策略网络加持时，棋力不一定优于优秀的启发式 Alpha-Beta
- 落地复杂度高
- 需要大量参数调优：
  - exploration constant
  - rollout 次数
  - 扩展策略
  - 终止策略

结论：

- 作为第三阶段实验方案，不建议第一阶段直接切换

### 路线 C：混合策略（Opening Heuristic + Alpha-Beta + MCTS）

策略建议：

- 开局：模式库/中心控制启发式
- 中局：增强版 Alpha-Beta
- 大棋盘复杂局面或高规格实验模式：时间受限 MCTS 辅助

优点：

- 更符合不同阶段的局面特征
- 能平衡棋力与响应时间

缺点：

- 实现复杂度最高
- 调试难度较大

结论：

- 作为长期目标路线

## 7.4 推荐 AI 升级路线

### 第一优先路线

采用“**增强版 Minimax/Alpha-Beta**”作为正式扩展方案，先打通 `13x13` 和 `15x15`。

### 第二优先路线

在 `19x19` 实验模式中评估“**Alpha-Beta + MCTS 混合策略**”。

## 7.5 AI 关键优化方案

### A. 分规格参数模板

建议建立规格参数表：

| 规格 | 时间预算 | 目标深度 | 候选数上限 | 候选半径 | 策略 |
| --- | --- | --- | --- | --- | --- |
| `9x9` | `800~1200ms` | `4~6` | `20` | `2` | 现有增强版 Alpha-Beta |
| `13x13` | `1200~2000ms` | `3~5` | `18~24` | `2` | 增强版 Alpha-Beta |
| `15x15` | `1800~3000ms` | `3~4` | `14~20` | `2` | 增强版 Alpha-Beta + 缓存 |
| `19x19` | `2500~3000ms` | `2~4` | `12~18` | `1~2` | 混合策略/实验模式 |

### B. 候选点生成升级

推荐从“半径邻近空点”升级为“多路打分候选”：

- 必胜点
- 必堵点
- 冲四点
- 活三点
- 中心偏好
- 双向延展潜力

只保留 Top K 候选，而不是简单枚举所有邻近空位。

### C. Move Ordering 升级

排序信号建议：

- 是否直接成五
- 是否能堵对方成五
- 当前点评估分
- 上轮迭代主变优先
- 历史启发命中频次

### D. 缓存与复用

建议引入：

- Zobrist Hash
- Transposition Table
- 局部线段评估缓存

目标：

- 减少重复局面评估
- 提高相邻分支搜索复用率

### E. 启发式评估函数升级

需按大棋盘特征重新调权：

- 中央控制的权重在开局更高
- 多威胁并存优先级高于单线延展
- 远离主战场的孤立落子要降权
- 对开放四、双活三、复合威胁加大惩罚和奖励

### F. 超时控制与用户体验保障

目标阈值：

- 默认单步响应时间：`<= 3 秒`
- 若超预算，必须优雅降级：
  - 立即返回当前已完成迭代的最佳着法
  - 不允许阻塞至不可接受时长

推荐策略：

- 保留迭代加深
- 每一层都持有“可立即返回”的最优步
- 按规格动态选择最大深度与时间预算

## 7.6 棋力不明显下滑的保障措施

需要定义“明显下滑”的工程判据：

- AI 不应频繁漏掉一步必胜
- AI 不应频繁漏掉一步必堵
- 在同规格自对弈中不出现明显随机偏移
- 对标准基准局面保持合理命中率

建议建立三类 AI 基准题库：

1. 必胜一步
2. 必堵一步
3. 中盘布局选择

以此衡量扩展后棋力是否可接受。

---

## 8. 分步实施规划

## 8.1 第一阶段：落地 `13x13`

### 目标

- 在当前架构下优先支持 `13x13`
- 不更换主算法路线
- 完成前后端多规格能力和 AI 参数化改造

### 核心任务

1. 前端增加规格选择并透传 `size`
2. 棋盘渲染、文案、分析说明动态化
3. 后端按规格输出统一状态
4. AI 引入分规格参数模板
5. 候选点排序与评估函数做第一轮增强
6. 建立 `13x13` 基本测试与性能基线

### AI 适配要求

- 单步响应目标：`<= 2 秒`
- 保留必胜/必堵优先逻辑
- 在 `13x13` 上保持稳定中心开局与中盘防守能力

### 交付标准

- 用户可稳定创建 `13x13` 棋局
- 人机对弈流程完整可用
- AI 落子不会频繁超时

## 8.2 第二阶段：落地 `15x15`

### 目标

- 形成主推标准规格
- 让 `15x15` 成为面向普通用户的正式模式

### 核心任务

1. 引入更强的 move ordering
2. 引入 transposition table
3. 调整候选点筛选策略
4. 优化前端大棋盘显示体验
5. 建立 `15x15` 性能与棋力基准

### AI 适配要求

- 单步响应目标：P90 `<= 3 秒`
- 必胜/必堵命中率维持高水平
- 中盘不出现明显“只看局部两格”的弱智走法

## 8.3 第三阶段：实验支持 `19x19`

### 目标

- 以实验模式接入大棋盘
- 评估混合搜索策略

### 核心任务

1. 引入 MCTS 试验实现或混合策略
2. 实现“高规格模式”性能保护机制
3. 增加用户提示：
   - 实验模式
   - 可能较慢
   - 可选快速 AI / 标准 AI

### AI 适配要求

- 在 `<= 3 秒` 预算下提供可接受棋力
- 无法满足时可降级为实验能力，不作为默认主推规格

---

## 9. 测试与质量保障规划

## 9.1 棋盘功能完整性测试

覆盖内容：

- 不同规格棋局创建
- 落子合法性
- 棋盘边界点击与边界判定
- 满盘平局
- 横、竖、斜向成五判断
- 规格切换后前端显示正确

重点规格：

- `9x9`
- `13x13`
- `15x15`
- `19x19`（实验）

## 9.2 边界场景校验

场景包括：

- 四角落子
- 边线落子
- 极小剩余空位
- 复盘接口在不同规格下的坐标显示
- AI 超时回退
- 快速连续点击时的状态一致性

## 9.3 AI 棋力基准测试

建立基准棋局集：

- 必胜一步题
- 必堵一步题
- 活三/冲四取舍题
- 开局布局题

指标建议：

- 必胜命中率
- 必堵命中率
- 中盘关键手命中率
- 自对弈稳定性

## 9.4 性能压测

重点关注：

- 各规格单步响应时间
- 节点数分布
- 深度分布
- 峰值时长

建议统计：

- P50
- P90
- P99

目标：

- `9x9`：P90 `<= 1.5s`
- `13x13`：P90 `<= 2.0s`
- `15x15`：P90 `<= 3.0s`
- `19x19`：实验模式，不作为正式 SLA

## 9.5 前端可用性测试

包括：

- 小屏幕和普通笔记本下的大棋盘可读性
- 点击热区准确性
- AI 搜索过程中的按钮状态
- 规格切换后的对话和棋局面板兼容性

---

## 10. 推荐决策

基于当前项目代码结构、已有算法路线和用户体验目标，推荐如下：

### 正式落地路线

1. 第一阶段优先上线 `13x13`
2. 第二阶段上线 `15x15`
3. `9x9` 保留为快速模式
4. `19x19` 延后至实验模式

### AI 路线决策

- 正式路线：增强版 `Minimax + Alpha-Beta + 迭代加深 + 候选筛选 + 缓存`
- 实验路线：`MCTS` 或混合搜索策略

### 最大尺寸决策

- 当前架构的推荐正式上限：`15x15`
- `19x19` 仅建议在第三阶段作为实验规格评估

---

## 11. 风险与应对

| 风险 | 说明 | 应对策略 |
| --- | --- | --- |
| 搜索时间失控 | 棋盘扩大后搜索树急剧膨胀 | 分规格参数、迭代加深、严格超时回退 |
| AI 棋力下降 | 深度不足导致中盘判断变差 | 候选排序、评估函数重调、缓存优化 |
| 前端可视性下降 | 大棋盘下格子变小 | 响应式布局、缩放、自适应棋子尺寸 |
| 测试覆盖不足 | 大量边界场景未覆盖 | 建立多规格测试矩阵和基准题库 |
| 19x19 体验不达标 | 当前架构难同时保证棋力和时延 | 延后为实验模式，不作为正式承诺 |

---

## 12. 总结

当前项目在后端数据结构层面已经具备从 `9x9` 向多规格扩展的基础，但产品体验、前端渲染、AI 参数和测试体系仍然是围绕 `9x9` 固化设计。若希望扩展为具备真实多规格能力的五子棋系统，最合理的推进路径不是一次性跳到 `19x19`，而是按如下顺序演进：

- 先完成 `13x13` 的产品级支持
- 再把 `15x15` 做成正式主推规格
- 最后将 `19x19` 作为实验能力，通过混合 AI 路线评估可行性

在算法层面，应优先沿当前代码基础升级增强版 `Alpha-Beta` 路线，而不是在第一阶段直接切换到 MCTS。这样能以最低改造风险达成“多规格 + AI 稳定 + 单步响应 <= 3 秒”的工程目标。
